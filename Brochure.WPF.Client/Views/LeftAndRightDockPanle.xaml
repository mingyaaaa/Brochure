<UserControl x:Class="Brochure.WPF.Client.Views.LeftAndRightDockPanle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Row="0" Grid.Column="0">
            <Grid>
                <Button Content="aaaaa"></Button>
            </Grid>
        </Border>
        <Border Grid.Row="0" Grid.Column="2">
            <Grid>
                <Button Content="aaaaa"></Button>
            </Grid>
        </Border>
        <GridSplitter Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ResizeBehavior="BasedOnAlignment" ResizeDirection="Columns"></GridSplitter>
    </Grid>
</UserControl>
